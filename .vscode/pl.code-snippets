{
  // Place your cos3103 workspace snippets here. Each snippet is defined under a snippet name and has a scope, prefix, body and
  // description. Add comma separated ids of the languages where the snippet is applicable in the scope field. If scope
  // is left empty or omitted, the snippet gets applied to all languages. The prefix is what is
  // used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
  // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders.
  // Placeholders with the same ids are connected.
  // Example:
  // "Print to console": {
  // 	"scope": "javascript,typescript",
  // 	"prefix": "log",
  // 	"body": [
  // 		"console.log('$1');",
  // 		"$2"
  // 	],
  // 	"description": "Log output to console"
  // }
  "Wrap with c`": {
    "prefix": "c`",
    "body": ["```c", "$0", "```"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with js`": {
    "prefix": "js`",
    "body": ["```js", "$0", "```"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with cpp`": {
    "prefix": "cpp`",
    "body": ["```cpp", "$0", "```"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with java`": {
    "prefix": "java`",
    "body": ["```java", "$0", "```"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with python`": {
    "prefix": "python`",
    "body": ["```python", "$0", "```"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with swift`": {
    "prefix": "swift`",
    "body": ["```swift", "$0", "```"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with pascal`": {
    "prefix": "pascal`",
    "body": ["```pascal", "$0", "```"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with text-2xl": {
    "prefix": "t2xl",
    "body": ["[$0]{class=\"text-2xl\"}"],
    "description": "Wraps selected text with defined strings"
  },
  "Wrap with imgw": {
    "prefix": "imgw",
    "body": ["<div class=\"w-[100%] mx-auto\">", "", "$0", "</div>"],
    "description": "Wraps selected text with defined strings"
  },
  "Two-cols-title layout": {
    "prefix": "tcl-",
    "body": [
      "---",
      "layout: two-cols-title",
      "---",
      "",
      "::title::",
      "[$0]{class=\"text-2xl\"}",
      "",
      "::left::",
      "",
      "::right::",
      "",
      "::default::"
    ],
    "description": "Two cols title layout"
  }
}
